services:
  gateway:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: qris-payment-gateway
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
    volumes:
      - ./data:/app/data
